// @flow
import React from "react";

/**
 * Render append
 */
const InputAppend = ({ children }: { children?: any }) => (
  <div className="input-group-append">{children}</div>
);

export default InputAppend;
